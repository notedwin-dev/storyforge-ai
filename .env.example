# StoryForge AI Environment Configuration

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-here-change-this-in-production

# Supabase Configuration
SUPABASE_URL=your-supabase-url
SUPABASE_ANON_KEY=your-supabase-anon-key

# Gemini AI Configuration
GEMINI_API_KEY=your-gemini-api-key-here

# === FREE IMAGE GENERATION (RECOMMENDED) ===
# Python-based local Stable Diffusion - NO COST!
USE_PYTHON_SD=true
PYTHON_SD_URL=http://127.0.0.1:8080
PREFER_PYTHON_SD=true

# === PAID IMAGE GENERATION (BACKUP) ===
# Hugging Face for cloud fallback - COSTS MONEY!
ENABLE_CLOUD_FALLBACK=true
HUGGING_FACE_TOKEN=your-hugging-face-token-here

# === COST OPTIMIZATION SETTINGS ===
# Recommended for cost savings:
# USE_PYTHON_SD=true (FREE local generation)
# PREFER_PYTHON_SD=true (try free first)
# ENABLE_CLOUD_FALLBACK=true (backup if local fails)
# 
# Cost comparison:
# - Python SD: $0 per image (after setup)
# - Cloud SD: $0.003-0.01 per image ($12-40/month for heavy use)